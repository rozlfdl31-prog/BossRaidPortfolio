# 2026-02-28 (토): 보스 모델 동기화 이슈 기록 (.gitignore 규칙 수정 필요)

## 오늘 반영한 작업
* 다른 환경에서 `Assets/FourEvilDragonsPBR/Prefab/DragonUsurper/Red.prefab` 누락이 발생하는 원인을 확인하고, `.gitignore` 규칙 수정 필요사항을 문서화했다.
* 장기 백로그(`docs/roadmap/Milestone_Backlog.md`)에 `.gitignore` 수정 작업을 버그 항목으로 등록했다.
* Progress Log 인덱스(`docs/Progress_Log/README.md`)에 금일 로그를 추가했다.

## 체크리스트 업데이트
* [x] 모델 누락 원인 기록: `/[Aa]ssets/FourEvilDragonsPBR/` ignore 규칙 확인
* [x] 일지/마일스톤에 규칙 수정 필요사항 반영
* [ ] 작업 PC에서 `.gitignore` 규칙 수정 후 `Red.prefab`/`Red.prefab.meta` 추적 전환

## 맥락노트
* 현재 저장소는 `Assets/FourEvilDragonsPBR/` 경로가 ignore되어 있어 모델/프리팹 변경 이력이 Git으로 공유되지 않는다.
* 이 상태가 유지되면 컴퓨터별 에셋 상태가 달라져 Lunge 연출/판정 검증이 환경마다 달라질 수 있다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: 미실행
* 결과: 문서 변경만 수행
* 미실행 사유 및 대체 검증: 코드 변경이 없어 빌드를 실행하지 않았다.

## 기술적 고려
### 무엇을 발견했는가
* `Assets/FourEvilDragonsPBR/Prefab/DragonUsurper/Red.prefab` 및 `.meta`가 Git 추적 대상이 아니어서 변경 흔적이 남지 않았다.

### 무엇을 수정했는가
* Progress Log와 Milestone Backlog에 `.gitignore` 규칙 수정 필요성과 후속 작업 항목을 명시했다.

### 왜 그렇게 판단했는가
* 에셋 누락 원인과 후속 조치를 문서에 고정해 두어야, 다음 환경에서 동일 문제를 반복하지 않고 바로 수정을 진행할 수 있다.
