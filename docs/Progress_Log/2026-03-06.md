# 2026-03-06 (금): Boss Attack4 AoE 반경 판정 규칙 수정 & 카메라 수정

# Boss Attack4 AoE 반경 판정 규칙 수정
## 오늘 반영한 작업
* Attack4 AoE 데미지 타이밍을 fully red(active) 구간 기준으로 정리했다.
* `AoECircleController`의 active phase 틱 루프를 제거하고, fully red 동안 반경 내 대상을 프레임마다 검사하되 circle당 1회만 적용하도록 변경했다.
* `EnterActivePhase()` 진입 시 즉시 1회 반경 판정을 수행해 fully red 시작 프레임 타이밍 오차를 줄였다.
* `ReceiveBossAttackHit` 결과가 `Ignored`(invul)인 경우 hit consumed 처리하지 않고, fully red 구간 내에서는 invul 종료 후 1회 피격 가능하게 유지했다.
* AoE 데미지 소스는 기존 `AoEAttackSettings.damage`를 유지했다.

## 체크리스트 업데이트
* [x] Attack4 AoE fully red 구간 반경 판정 적용
* [x] one circle, one hit 규칙 적용
* [x] fully red 시작 프레임 즉시 판정 적용
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

## 맥락노트
* 요청 스펙의 핵심은 `one-time hit`와 `fully red 중 later entry도 피격`의 동시 만족이다.
* 구현 정책은 "active window one-hit per target per circle"로 고정했다.
* Attack4는 기존 `BossAttackHitType.Attack4Projectile` 경로를 유지해 플레이어 stun/post-stun invul 처리와 동일 파이프라인을 사용한다.

## 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v minimal`
* 결과: 성공 (Warning 3, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

## 기술적 고려
### 무엇을 발견했는가
* 기존 AoE는 active phase에서 `tickInterval` 기준 반복 데미지를 적용했고, tick마다 hit set을 초기화해 same circle 재피격 가능성이 있었다.
* telegraph 종료 프레임과 첫 데미지 프레임 사이에 1프레임 이상 지연이 발생할 수 있었다.

### 무엇을 수정했는가
* active phase 반복 틱 루프를 제거하고, fully red 동안 프레임 단위 반경 검사로 전환했다.
* circle 생명주기 단위 hit set 유지로 same circle 재피격을 차단했다.
* `EnterActivePhase()`에서 즉시 1회 판정을 추가했다.
* `AoEAttackPattern`의 `StartTelegraph` 호출 시그니처를 업데이트했다(틱 인자 제거).

### 왜 그렇게 판단했는가
* 요구사항은 projectile 충돌이 아니라 AoE radius + fully red 기준 판정이다.
* later entry 피격과 one circle one hit을 동시에 만족하려면 active window 동안 신규 대상만 1회 적용하는 방식이 가장 직접적이다.
* invul 처리 일관성을 위해 기존 `BossAttackHitResolution` 경로를 그대로 재사용하는 것이 회귀 위험이 가장 낮다.

---

## 추가 반영 (Attack3/Attack4 projectile combo 유지 + 피해 누락 보정)

### 오늘 반영한 작업
* `PlayerController.HandleProjectileHit`를 수정해 projectile combo를 유지하면서도 유효 피격마다 HP damage가 적용되도록 변경했다.
* 2타 이상에서는 stun을 발생시키되, 해당 타격의 damage도 함께 적용되도록 조정했다.

### 체크리스트 업데이트
* [x] projectile combo 유지
* [x] projectile combo 2타 이상 stun + damage 동시 적용

### 기술적 고려
#### 무엇을 발견했는가
* 기존 구현은 2타 이상에서 `StunOnly`를 반환해 HP damage가 빠질 수 있었다.

#### 무엇을 수정했는가
* combo 타이머/카운트 구조는 유지하고, damage 성공 시 항상 `BossAttackHitResolution.Damaged`를 반환하도록 수정했다.
* 2타 이상에서는 stun 트리거 후 카운터를 리셋하되, 해당 타격 damage는 유지했다.

#### 왜 그렇게 판단했는가
* 사용자 요구는 combo 유지이며, AoE circle/Projectile 어느 경로든 "피격 시 damage가 보이는 것"이 우선이다.
* 공격 분류를 분리하는 대신 기존 공통 projectile 경로를 보정하는 편이 변경 범위가 작고 효율적이다.

---

## 추가 반영 (코드 읽기 규칙 문서화: 6-Line Trace Card)

### 오늘 반영한 작업
* 코드 읽기/버그 분석용 공통 규칙 `6-Line Trace Card`를 문서에 추가했다.
* 카드 고정 6단계(`Trigger -> Entry -> Gate -> Core Check -> Effect -> Result`)와 라인 포맷(`[S#] Action | Condition | File:line | Key value`)을 명시했다.
* 단일 동작 범위 원칙(`Single-Behavior Scope`)을 함께 정의했다.

### 체크리스트 업데이트
* [x] AI 가이드에 Trace Card 규칙 추가
* [x] Coding Standard에 Traceability 규칙 추가
* [x] Technical Glossary 용어 추가

### 맥락노트
* 사용자가 라인 단위 추적을 어려워했기 때문에, 설명 방식보다 기록 포맷을 먼저 표준화했다.
* 동일 규칙을 Guide/Standard/Glossary에 동시에 반영해 실사용 시 문서 간 용어 충돌을 줄였다.

### 기술적 고려
#### 무엇을 발견했는가
* 기존 문서에는 "코드를 어떻게 읽을지"에 대한 고정 추적 포맷이 없었다.

#### 무엇을 수정했는가
* `6-Line Trace Card` 규칙과 라인 작성 템플릿을 문서 표준으로 추가했다.

#### 왜 그렇게 판단했는가
* 같은 버그라도 요약 방식이 매번 달라지면 학습 속도가 떨어진다.
* 고정 카드 포맷이 있으면 step 6(Result)까지 빠르게 도달하고, 디버깅 대상 라인을 더 정확히 고를 수 있다.

---

## 추가 반영 (AoE 시각 반경-데미지 반경 정렬)

### 오늘 반영한 작업
* `AoECircleController`에 `fallbackRadiusToScaleMultiplier`를 추가했다.
* 런타임 fallback 디스크를 사용하는 경우, 시각 반경 스케일 계산은 `fallbackRadiusToScaleMultiplier`를 사용하도록 변경했다.
* 기본값을 `1`로 설정해 fallback 디스크의 보이는 반경과 OverlapSphere 데미지 반경이 동일 참조를 사용하도록 맞췄다.

### 체크리스트 업데이트
* [x] fallback 시각 반경 스케일 분리
* [x] fallback 기본값 1 적용
* [x] 보이는 원 가장자리와 피해 반경 기준 정렬

### 기술적 고려
#### 무엇을 발견했는가
* 기존에는 fallback 디스크도 `radiusToScaleMultiplier`를 공유해, 시각 반경이 데미지 반경보다 크게 보일 수 있었다.

#### 무엇을 수정했는가
* fallback 경로에서 별도 multiplier를 사용하도록 분리했다.

#### 왜 그렇게 판단했는가
* 사용자 체감 기준은 "보이는 원의 경계"다.
* fallback 디스크는 기하가 단순(단위 반경 디스크)이므로 기본 multiplier=1이 가장 일관된 기준이다.

---

## 추가 반영 (용어 정리: telegraph -> warning)

### 오늘 반영한 작업
* Attack3/Attack4 관련 코드에서 `telegraph` 용어를 `warning`으로 정리했다.
* 런타임 동작은 유지하고, 직렬화 필드에는 `FormerlySerializedAs`를 추가해 기존 씬/프리팹 값 호환성을 유지했다.
* `AoECircleController` 공개 API를 `StartTelegraph`에서 `StartWarning`으로 변경하고 호출부를 동기화했다.

### 체크리스트 업데이트
* [x] Attack3/Attack4 설정 용어를 `warningDuration`으로 통일
* [x] AoE circle API 용어를 `StartWarning`으로 통일
* [x] 직렬화 호환성(`FormerlySerializedAs`) 유지
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

### 맥락노트
* 사용자가 `telegraph` 단어를 혼동했기 때문에, 같은 의미의 쉬운 단어(`warning`)로 코드 읽기 난이도를 낮추는 것이 우선이었다.
* 런타임 리스크를 줄이기 위해 값 의미와 타이밍 로직은 변경하지 않고 이름만 정리했다.

### 기술적 고려
#### 무엇을 발견했는가
* `telegraphDuration`과 `StartTelegraph` 같은 이름은 신규 개발자에게 의미 전달이 직관적이지 않았다.

#### 무엇을 수정했는가
* `telegraphDuration` -> `warningDuration`
* `StartTelegraph` -> `StartWarning`
* `_telegraphTimer`/`_telegraphDuration`/`telegraphRenderer`/`telegraphColor` -> `warning*` 계열

#### 왜 그렇게 판단했는가
* 변경 목표는 "동작 변경"이 아니라 "읽기 쉬운 의미 전달"이다.
* 이름 정리 + 직렬화 호환을 같이 적용하면 유지보수성과 안정성을 동시에 확보할 수 있다.

---

## 추가 반영 (Attack4 타이밍 단일화: warningDuration only)

### 오늘 반영한 작업
* Attack4에서 `impactSyncTime` 사용을 제거하고 `warningDuration` 하나만 타이밍 기준으로 사용하도록 변경했다.
* AoE circle 경고 종료 시점과 fire projectile 착지 시점을 같은 값(`warningDuration`)으로 강제 동기화했다.
* `AoEAttackSettings`에서 `impactSyncTime` 필드를 제거하고 툴팁을 단일 기준에 맞게 정리했다.

### 체크리스트 업데이트
* [x] Attack4 타이밍 소스를 `warningDuration` 단일 값으로 통일
* [x] circle warning 종료와 projectile impact 동기화 유지
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

### 맥락노트
* 요구사항은 "둘을 맞춘다"를 넘어서 "하나의 값으로 관리"하는 것이다.
* 두 필드가 공존하면 언젠가 다시 드리프트가 생길 수 있으므로, 설정 자체를 단일화했다.

### 기술적 고려
#### 무엇을 발견했는가
* 기존 구조는 `impactSyncTime > 0`일 때 `warningDuration`과 다른 값을 사용할 수 있어 설정 드리프트 가능성이 있었다.

#### 무엇을 수정했는가
* `AoEAttackPattern`에서 `_impactSyncTime` 경로를 제거하고 `_warningDuration`만 사용하도록 수정했다.
* circle 시작(`StartWarning`)과 projectile impact marker 초기화 모두 동일한 `_warningDuration`을 전달하도록 변경했다.

#### 왜 그렇게 판단했는가
* 단일 소스는 디버깅과 밸런싱이 단순하다.
* 인스펙터에서 "warningDuration 하나만 조정하면 된다"는 규칙이 명확해진다.

---

## 추가 반영 (UX 튜닝: fallback visual edge 1.2)

### 오늘 반영한 작업
* fallback 원형 시각 배수를 `1.0`에서 `1.2`로 조정했다.
* 물리 데미지 반경(`radius`)은 그대로 유지하고, 시각 경계를 조금 크게 보여주는 보수적 경고 UX로 전환했다.

### 체크리스트 업데이트
* [x] fallback visual multiplier `1.2` 적용
* [x] 데미지 반경 로직 변경 없음 (`distance <= radius`)
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

### 맥락노트
* 실플레이에서 일부 각도에서 "원 밖에서 맞는 느낌"이 보고됐다.
* UX 관점에서는 실제 위험보다 경고를 약간 크게 보여주는 편이 불공정 체감을 줄인다.

### 기술적 고려
#### 무엇을 발견했는가
* 카메라 각도/원근/캐릭터 체적 체감 때문에 경계 근처에서 시각적으로 원 밖 피격처럼 보일 수 있었다.

#### 무엇을 수정했는가
* `AoECircleController.fallbackRadiusToScaleMultiplier` 기본값을 `1.2`로 올렸다.

#### 왜 그렇게 판단했는가
* 판정 정확도보다 플레이어 체감 공정성이 우선인 UX 튜닝 구간이다.
* 경고를 더 크게 보여주면 "안 보이는 피격" 불만을 줄일 수 있다.

---

# 카메라 수정

## 추가 반영 (플레이어, 카메라 리팩토링: Cinemachine auto-behind)

### 오늘 반영한 작업
* `PlayerController`에 Cinemachine 기반 auto-behind 카메라 초기화 경로를 추가했다.
* 런타임 시작 시 `CameraRoot`를 플레이어 자식 계층에서 분리하고, `LateUpdate`에서 위치/Yaw를 부드럽게 추적하도록 변경했다.
* `Main Camera`에 `CinemachineBrain`을 보장하고, `CinemachineVirtualCamera`를 생성/재사용해 `Follow/LookAt`을 `CameraRoot`에 연결했다.
* 기존 `Update`의 `cameraRoot.rotation = Quaternion.Euler(input.lookPitch, input.lookYaw, 0f)` 경로는 manual 모드에서만 동작하도록 분기했다.
* 이동 방향 계산(`GetMovementDirection`)에 `cameraRoot` null fallback을 추가해 런타임 안정성을 높였다.
* 문서 동기화: `System_Blueprint`, `Technical_Glossary`를 업데이트했다.

### 체크리스트 업데이트
* [x] CameraRoot parent 분리(런타임) 적용
* [x] Cinemachine VCam Follow/LookAt auto-behind 구성 적용
* [x] 카메라 앵커 갱신 시점을 `LateUpdate`로 이동
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

### 맥락노트
* 기존 구조(`Player/CameraRoot/Main Camera`)에서는 플레이어 회전과 카메라 회전이 같은 부모 체인에 묶여 좌우 회전 시 화면 지터가 발생했다.
* 이번 변경은 "camera under player" 결합을 끊고, 이동 기준 축은 유지하면서 카메라 추적만 Cinemachine에 위임하는 방식으로 범위를 최소화했다.

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v minimal`
* 결과: 성공 (Warning 3, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

### 기술적 고려
#### 무엇을 발견했는가
* 카메라가 플레이어 자식일 때, 플레이어 회전 입력과 카메라 기준 이동 벡터가 서로 영향을 주며 좌우 회전에서 떨림이 커졌다.
* 기존 Player FSM은 `cameraRoot.forward/right` 기반 이동에 이미 결합되어 있어, 카메라 리팩토링 시 이동 기준 축 보존이 중요했다.

#### 무엇을 수정했는가
* `PlayerController`에 `InitializeCameraRigIfNeeded`, `UpdateCameraRootAnchor`, `SetupCinemachineVirtualCamera`를 추가했다.
* `Start`에서 카메라 리그를 1회 초기화하고, `LateUpdate`에서 `CameraRoot` 앵커를 스무딩 추적하도록 변경했다.
* main camera parent 분리 + `CinemachineBrain` 보장 + runtime virtual camera 구성(`Transposer`/`Composer` damping)을 적용했다.

#### 왜 그렇게 판단했는가
* 문제의 핵심은 입력 보간보다 hierarchy coupling(부모 회전 상속)이었다.
* 기존 Player FSM/입력 패킷 구조를 크게 바꾸지 않으면서 지터를 줄이려면, `CameraRoot` 분리 + Cinemachine damping 조합이 가장 리스크가 낮다.

---

## 추가 반영 (카메라 2차 튜닝: Fortnite 느낌 강화)

### 오늘 반영한 작업
* `PlayerController`의 auto-behind yaw 정렬을 이동 입력 기반(move-gated)으로 변경했다.
* 플레이어가 정지 중일 때는 카메라 yaw를 강제로 재중앙정렬하지 않도록 수정했다.
* yaw 오차(`DeltaAngle`)가 클수록 더 빠르게 따라오도록 adaptive smooth time(급회전 추종)을 추가했다.
* 기본 damping 값을 낮춰 카메라 지연감을 줄였다(`Follow 0.14`, `Aim 0.05`).

### 체크리스트 업데이트
* [x] 이동 입력 기반 auto-align 적용
* [x] 급회전 adaptive yaw catch-up 적용
* [x] 기본 damping 하향 튜닝 적용
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

### 맥락노트
* 기존 1차 적용은 지터 제거에 집중했고, 체감상 "카메라가 늦게 따라오는" 구간이 남을 수 있었다.
* 이번 2차 튜닝은 회전 응답성을 높이되, 정지 중 불필요한 카메라 보정 움직임을 줄여 자연스러운 뒤따름 느낌을 목표로 했다.

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v minimal`
* 결과: 성공 (Warning 3, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

### 기술적 고려
#### 무엇을 발견했는가
* 고정 smooth time은 급회전 상황에서 카메라 추종이 느려 보일 수 있었다.
* 정지 중에도 yaw 정렬이 계속 동작하면 미세한 흔들림이나 "자동으로 끌려가는 느낌"이 발생할 수 있었다.

#### 무엇을 수정했는가
* 이동 입력 크기(`cameraAutoAlignMoveThreshold`)를 기준으로 yaw 정렬 동작을 게이트 처리했다.
* `cameraSharpTurnAngleThreshold`, `cameraSharpTurnYawSmoothTime`를 추가해 yaw 오차 기반 adaptive smooth time을 적용했다.
* 기본값을 빠른 추종에 맞게 조정했다.

#### 왜 그렇게 판단했는가
* 사용자가 원하는 체감은 "카메라 조작"보다 "캐릭터 이동 중심"이다.
* move-gated + adaptive catch-up 조합이 구현 비용 대비 체감 개선 효과가 가장 크고, 기존 FSM/입력 구조 회귀 위험이 낮다.


---

## 추가 반영 (카메라 책임 분리: Main Camera 전용 스크립트)

### 오늘 반영한 작업
* 카메라 기능을 `PlayerController`에서 분리하고, 메인 카메라 전용 `ThirdPersonCameraController`를 추가했다.
* `PlayerController`는 카메라 갱신 로직을 제거하고 `lookYaw/lookPitch/moveMagnitude` 캐시 + `SetCameraRoot` 주입만 담당하도록 경량화했다.
* 카메라 스크립트에서 마우스 yaw/pitch를 1차 입력으로 사용하도록 복구했다.
* auto-behind 보조 정렬 옵션을 `Off/Soft/Strong`으로 제공하고, 이동 중에만 보정이 적용되도록 구성했다.
* `followOffset`, damping, pitch clamp, assist 강도 등 카메라 설정을 메인 카메라 컴포넌트 인스펙터로 이동했다.

### 체크리스트 업데이트
* [x] 카메라 기능 Main Camera 전용 스크립트로 분리
* [x] PlayerController 카메라 경량화(look cache + CameraRoot 주입)
* [x] 마우스 카메라 제어 복구(yaw/pitch primary)
* [x] Assist Mode `Off/Soft/Strong` 옵션 제공
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

### 맥락노트
* 사용자 요구의 핵심은 "카메라 설정은 카메라에서 관리"와 "마우스 직접 제어 복구"였다.
* 기존 Player 기반 카메라 구조는 책임 위치가 어긋나 있어 튜닝 반복 시 유지보수 비용이 높았다.

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v minimal`
* 결과: 성공 (Warning 2, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

### 기술적 고려
#### 무엇을 발견했는가
* 카메라 로직이 PlayerController 내부에 있으면 카메라 튜닝과 플레이어 상태 로직이 강결합된다.
* 사용자는 마우스 수동 시점 제어를 기본으로 원했고, auto-behind는 보조 동작이어야 했다.

#### 무엇을 수정했는가
* `Assets/Scripts/Camera/ThirdPersonCameraController.cs` 신규 추가.
* `PlayerController`에서 카메라 회전/추적/Cinemachine 초기화 코드를 제거하고 input cache + `SetCameraRoot` API만 남겼다.
* 카메라 컴포넌트에서 `Off/Soft/Strong` 보조 정렬 옵션과 move-gated, sharp-turn catch-up 규칙을 처리하도록 변경했다.

#### 왜 그렇게 판단했는가
* 카메라 기능을 카메라 오브젝트로 이동하면 요구 변경(감도, 오프셋, 보조 정렬 강도)에 대한 실험 사이클이 빨라진다.
* Player FSM 회귀 리스크를 최소화하면서 카메라 체감 문제를 직접 해결할 수 있다.

#### 추후 조치
* Main Camera(또는 Camera Rig)에 카메라 전용 새 스크립트로 모든 카메라 기능을 이동한다.
* PlayerController는 카메라 경량 상태를 유지하고, 카메라 스크립트로 look 입력 데이터만 전달한다.
* 마우스 yaw/pitch 제어를 기본 동작(primary)으로 복구한다.
* 선택형 auto-behind 보조 기능은 카메라 인스펙터 토글로 유지한다.
* Follow Offset, damping, pitch clamp, assist strength 등 모든 튜닝 값은 카메라 컴포넌트에서 관리한다.

---

## 추가 반영 (카메라 UX 개선: 에디터 표시 + 쉬운 툴팁)

### 오늘 반영한 작업
* `ThirdPersonCameraController`가 플레이 모드에서만 보이던 문제를 수정했다.
* 에디터 로드 시점(`InitializeOnLoadMethod`)에도 `Main Camera`에 카메라 컨트롤러가 자동 부착되도록 보강했다.
* `Auto-Behind Assist` 관련 필드(`assistMoveThreshold`, `soft/strongAssistStrength`, `sharpTurn*`)에 쉬운 영어 툴팁을 추가했다.
* Follow/Look 필드(`followOffset`, damping, pitch clamp)에도 쉬운 영어 툴팁을 추가해 인스펙터 이해도를 높였다.

### 체크리스트 업데이트
* [x] 에디터에서 카메라 컨트롤러 즉시 노출
* [x] Auto-Behind Assist 쉬운 영어 툴팁 추가
* [x] Follow/Look 필드 툴팁 추가
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v minimal`
* 결과: 성공 (Warning 3, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

### 기술적 고려
#### 무엇을 발견했는가
* 런타임 자동 부착만 있으면 인스펙터 튜닝을 플레이 모드 전에 수행하기 어렵다.
* assist 계열 파라미터 이름만으로는 의미를 바로 파악하기 어렵다.

#### 무엇을 수정했는가
* 런타임 + 에디터 양쪽 자동 부착 경로를 모두 제공했다.
* 카메라 핵심 필드에 쉬운 영어 툴팁을 추가했다.

#### 왜 그렇게 판단했는가
* 사용자 튜닝 루프(값 조정 -> 즉시 확인)를 빠르게 만들려면 에디터 노출이 필수다.
* 툴팁은 코드 구조를 바꾸지 않고 학습 비용을 가장 빠르게 낮춘다.

---

## 추가 반영 (카메라 인스펙터 단순화: 불필요 속성 제거)

### 오늘 반영한 작업
* `ThirdPersonCameraController` 인스펙터에서 `Position Smooth Time`, `Rotation Smooth Time` 노출을 제거했다.
* `Auto-Behind Assist` 전체 옵션/필드를 제거했다.
* 카메라는 `lookYaw/lookPitch`만 사용하는 mouse primary 제어로 고정했다.
* 위치/회전 스무딩은 스크립트 내부 고정 상수로 유지해 인스펙터를 단순화했다.
* `PlayerController`에서 카메라로 전달하던 `LatestMoveMagnitude`를 제거했다.

### 체크리스트 업데이트
* [x] Position/Rotation Smooth Time 인스펙터 노출 제거
* [x] Auto-Behind Assist 관련 인스펙터/로직 제거
* [x] PlayerController 카메라 입력 데이터 경량화(lookYaw/lookPitch)
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v minimal`
* 결과: 성공 (Warning 3, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

### 기술적 고려
#### 무엇을 발견했는가
* 현재 사용자 워크플로에서는 assist/추가 스무딩 튜닝 필드가 오히려 인스펙터 복잡도를 높였다.

#### 무엇을 수정했는가
* 카메라 인스펙터 노출 필드를 최소 구성(필수 follow/look 값)으로 축소하고, 나머지는 내부 고정값으로 전환했다.

#### 왜 그렇게 판단했는가
* 반복 튜닝에서 "보여야 할 값"만 남기는 것이 작업 속도와 실수 방지에 유리하다.

---

## 추가 반영 (롤백: 카메라 인스펙터 튜닝 항목 복구)

### 오늘 반영한 작업
* 사용자 요청에 따라 `ThirdPersonCameraController`에서 숨겼던 튜닝 항목을 다시 인스펙터에 노출했다.
* `Position Smooth Time`, `Rotation Smooth Time` 값을 인스펙터에서 다시 조절할 수 있게 복구했다.
* `Auto-Behind Assist` 옵션(`Off/Soft/Strong`)과 관련 파라미터를 복구했다.
* `Auto-Behind Assist` 기본값을 `Off`로 설정했다.

### 체크리스트 업데이트
* [x] Position/Rotation Smooth Time 인스펙터 복구
* [x] Auto-Behind Assist 옵션/파라미터 복구
* [x] Assist 기본값 `Off` 적용
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v minimal`
* 결과: 성공 (Warning 3, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

### 기술적 고려
#### 무엇을 발견했는가
* 인스펙터 튜닝 필드를 숨기면 사용자 반복 조정 워크플로가 오히려 느려질 수 있다.

#### 무엇을 수정했는가
* 카메라 스무딩/assist 관련 인스펙터 항목을 다시 노출하고, assist 기본 모드를 `Off`로 되돌렸다.

#### 왜 그렇게 판단했는가
* 현재 요구사항의 핵심은 "기능 제거"가 아니라 "필요할 때 직접 수치 조정"과 "`Off` 기본값 유지"다.

---

## 추가 반영 (카메라 인스펙터 노출 정리: 데이터 유지 + 필드 숨김)

### 오늘 반영한 작업
* 사용자 요청에 따라 `smoothing`, `auto-behind assist` 항목을 인스펙터에서 숨김 처리했다.
* `ThirdPersonCameraController`의 해당 필드는 삭제하지 않고 직렬화 데이터를 유지했다.
* 런타임 동작 로직은 유지하고, 노출만 `HideInInspector`로 비활성화했다.

### 체크리스트 업데이트
* [x] smoothing 인스펙터 노출 제거(데이터 유지)
* [x] auto-behind assist 인스펙터 노출 제거(데이터 유지)
* [x] 코드 빌드 검증
* [x] Progress_Log/Blueprint/Glossary 문서 동기화

### 코드 검사 결과 (코드 변경 시 필수)
* 명령: `dotnet build Assembly-CSharp.csproj -v minimal`
* 결과: 성공 (Warning 3, Error 0)
* 미실행 사유 및 대체 검증: 해당 없음

### 기술적 고려
#### 무엇을 발견했는가
* 사용자는 튜닝 항목을 직접 보지 않기를 원하지만, 기존 값/동작 보존은 필요하다.

#### 무엇을 수정했는가
* 해당 필드를 삭제하지 않고 `SerializeField + HideInInspector`로 전환했다.

#### 왜 그렇게 판단했는가
* 이 방식은 씬/프리팹 직렬화 값을 잃지 않으면서 인스펙터 UI만 단순화할 수 있다.
